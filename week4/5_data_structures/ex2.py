# 추가 메소드가 필요할 떄 클래스 정의해서 사용
class Stack:
    def __init__(self):
        self.arr = []

    def push(el):
        self.arr.append(el)

    def pop():
        return self.arr.pop()


simple = []

simple.append(1)
simple.append(2)
simple.append(3)
simple.append(4)
simple.append(5)

print(simple.pop())
print(simple.pop())
print(simple.pop())
print(simple.pop())
print(simple.pop())
